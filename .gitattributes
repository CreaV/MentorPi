# 默认行为：让 Git 自动处理文本文件的换行符，并在最终存入版本库时统一使用 LF
* text=auto

# 明确指定哪些文件必须是 LF（如脚本文件，在 Linux 上运行必需）
*.sh text eol=lf
*.conf text eol=lf

# 明确指定哪些文件必须是 CRLF（如仅在 Windows 下使用的批处理脚本）
*.bat text eol=crlf
*.cmd text eol=crlf

# 明确指定哪些是二进制文件，Git 不该对其进行任何换行符转换
*.png binary
*.jpg binary